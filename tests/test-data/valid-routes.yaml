"/user/{userId}":
  "GET":
    body:
      parser: "string"
      schema:
        {
          $id: "https://example.com/body.json",
          $schema: "http://json-schema.org/draft-07/schema#",
          type: "object",
          properties: { body: { type: "string" } },
          required: ["body"]
        }
